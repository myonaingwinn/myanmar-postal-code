const LANGUAGE = Object.freeze({
  EN: 'en',
  MM: 'mm',
});

const COLUMNS = Object.freeze({
  REGION: 'Region',
  TOWN_TOWNSHIP: 'Town / Township',
  QUARTER_VILLAGE_TRACT: 'Quarter / Village Tract',
});

module.exports = {
  LANGUAGE,
  COLUMNS,
};
